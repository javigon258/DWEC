{
  function generaMensajes() {
    let apart1 = document.getElementById("conjunto");
    let regex = '\[, ]';
    
    }

  function init() {
    generaMensajes();
  }

  document.addEventListener("blur", init);
}
